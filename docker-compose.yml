version: '3.8'
services:
  postgres:
    image: postgres:15
    container_name: ledger_db
    environment:
      POSTGRES_DB: ledgerdb
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"