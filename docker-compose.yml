version: '3.8'

services:
  postgres:
    image: postgres:15
    container_name: ledger-db
    environment:
      POSTGRES_DB: ledger
      POSTGRES_USER: ledger_user
      POSTGRES_PASSWORD: ledger_secret_pass_123
    ports:
      - "5433:5432"